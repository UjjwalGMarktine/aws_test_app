# version: "3.9"

# services:
#   fastapi_app:
#     build: .
#     container_name: fastapi_app_container
#     ports:
#       - "8000:8000"
#     environment:
#       - PYTHONUNBUFFERED=1
#     restart: always


version: "3.9"

services:
  fastapi_app:
    build: .
    container_name: fastapi_app_container
    ports:
      - "80:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: always
